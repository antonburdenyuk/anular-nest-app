import { SignDirective } from './sign.directive';

describe('SignDirective', () => {
  it('should create an instance', () => {
    const directive = new SignDirective();
    expect(directive).toBeTruthy();
  });
});
